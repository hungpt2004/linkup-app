import 'package:flutter/widgets.dart';

class MyTitleDetailInformationWidget extends StatelessWidget {
  const MyTitleDetailInformationWidget({
    super.key,
    required this.imagePath,
    required this.text,
  });

  final String imagePath;
  final String text;

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        
      ],
    );
  }
}
